[wokwi]
version = 1
elf = ".esphome/build/test-device/.pioenvs/test-device/firmware.elf"
firmware = ".esphome/build/test-device/.pioenvs/test-device/firmware.bin"

# Forward http://localhost:8180 to port 80 on the simulated ESP32:
[[net.forward]]
from = "localhost:8180"
to = "target:80"