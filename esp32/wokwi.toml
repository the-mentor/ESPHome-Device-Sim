[wokwi]
version = 1
elf = ".esphome/build/test-esp32-device/.pioenvs/test-esp32-device/firmware.elf"
firmware = ".esphome/build/test-esp32-device/.pioenvs/test-esp32-device/firmware.bin"

# Forward http://localhost:8180 to port 80 on the simulated ESP32:
[[net.forward]]
from = "localhost:8180"
to = "target:80"
